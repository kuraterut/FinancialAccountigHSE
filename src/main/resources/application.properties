spring.application.name=TaskManagementSystemAPI

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/financialaccounting}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:H541xm76}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.security.filter.dispatcher-types=REQUEST, ASYNC, ERROR

server.port=8080

logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

springdoc.swagger-ui.enabled=true

logging.level.root=INFO
logging.file.name=app.log
logging.file.path=logs
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

springdoc.api-docs.title=Financial Accounting HSE API
springdoc.api-docs.description=API Financial Accounting HSE, as homework in HSE
springdoc.api-docs.version=openapi_3_0

spring.jackson.deserialization.fail-on-unknown-properties=true

